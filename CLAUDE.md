# CLAUDE.md

## 프로젝트 개요

**인천공항 경쟁숙소 가격·잔여 추적기**입니다. 우리 숙소(*Incheon Airport T1 Residence near Paradise City*, 평점 9.4, 부킹닷컴 기준, 총 8실)의 평일 공실을 채우는 것이 핵심 목표이며, 직접경쟁군(굿데이·호텔휴 등) 대비 가격·잔여를 매일 비교해 동적 추천가를 산출합니다.

단일 파일 프로젝트입니다 — `index.html` 하나에 HTML·CSS·순수 JavaScript가 모두 들어 있고, 외부 라이브러리나 빌드 도구는 없습니다.

## 구조 (2026-09 월별 재편 이후)

탭 6개: ①9월 ②10월 ③11월 ④12월 ⑤이벤트 캘린더 ⑥운영기준(접기, 변동로그도 이 안에 접혀 있음).

각 월 탭은 그 달 1일~말일을 하루도 빠짐없이 렌더하되(`renderMonthTab()`), 오늘(KST, 실제 시계 기준) 이전 날짜는 자동 숨김. 달이 통째로 지나면(`monthIsPast()`) 해당 탭 버튼이 회색(`.tab-past`)으로 흐려짐 — 탭 자체는 삭제하지 않으므로, 실제로 9~12월이 다 지나면(예: 내년 초) 탭 라벨/월 번호를 새 분기로 바꿔줘야 함.

가격표 컬럼은 **날짜 | 게하 | 굿데이 | 호텔휴 | 베웨 | 우리 현재가 | 우리 잔여 | 이벤트**로 고정(저가재고·경쟁군갭·추천가 컬럼은 삭제됨). 경쟁사 셀은 가격만 표시(`compCellSimple()`): 만실=완전매진, `-`=데이터 없음(스캔 안 함/미확정). 우리 현재가는 표시가+실수령(×0.85) 병기, 없으면 curNote(판매완료 등) 또는 `-`. 우리 잔여는 `N/객실` 또는 매진/`-`.

데이터는 `index.html` 안의 `<script>` 블록에 있는 JS 객체에 하드코딩되어 있습니다:

- **`TRACK`** — 날짜별 단일 객체(옛 DAILY/WEEKLY/AUGUST/SEPTEMBER 통합). 키는 `"M/D"`. 각 날짜에 경쟁 4숙소 `comp.{gh,gd,hu,bw}` (`{p,n,approx}` 실측잔여 · `{p,r:"free"}` 베웨 · `{p,r:"na"}` 잔여미확인 · `{p:null,r:"so"}` 매진), 우리 `our:{ourN,cur,curNote?}`, 선택적 `flag:{lvl,ic,txt}`(배지+툴팁), `note`(📝 아이콘 툴팁), `hl:"strong"|"chuseok"`(강세/추석 태그)를 담습니다.
  - **오늘(KST, `TODAY_KST`, 실제 시계 기준) 이전 날짜는 항상 삭제** — `ASOF_*` 상수가 아니라 실기기 날짜 기준으로 매일 자동 롤링되므로, `TRACK`엔 오늘 이후 날짜만 남겨둔다.
  - `TRACK`에 없는 날짜는 그 달 탭에서 모든 열이 `-`로 표시되고, `EVENTS`에 이벤트가 있으면 이벤트 배지만 뜬다(10~12월처럼 가격 데이터가 아직 없는 달의 기본 상태).
- **`EVENTS`** / **`POKER_EVENTS`** / **`POKER_BANDS`** — 이벤트 캘린더(⑤탭)용이자 각 월 탭의 이벤트 열의 데이터 소스. 지난 이벤트는 `TODAY_MS` 기준으로 렌더 시 자동 숨김(데이터는 안 지움 — 포커밴드 강도 근거 등 상호참조가 있어서).
- **`COMP_LOG`** / **`OURS_LOG`** — 조회 시각별 스냅샷(운영기준 탭에 접힌 변동로그가 사용). `pruneLog(log, 3)`이 매 로드 시 **최근 3개만** 자동으로 남기고 나머지는 삭제(더 오래된 이력은 git 커밋 기록으로 충분).

잔여 실측 표기: `n`(정확한 잔여수) · `approx:true`(드롭다운 상한 근사) · `r:"so"`(매진) · `r:"na"`(가격만, 잔여 미확인) · `r:"free"`(베웨, 대형·상시여유).

추천가·경쟁군갭·저가재고 자동계산 컬럼은 삭제됨(2026-09). 관련 철학(`dynRec` 로직 등)은 운영기준 탭에 "참고용, 자동계산 아님"으로만 남아있음 — 되살리지 말 것, 수동 판단 참고 자료임.

셀 안의 배지(🔴🟠🟢🎯 등)는 화면엔 아이콘만 보이고, 상세 설명은 `title` 속성(hover 툴팁)에 들어갑니다 — 새 flag/note를 추가할 때도 이 규칙을 유지할 것(긴 문장을 셀에 직접 풀어쓰지 않기).

## 매일 갱신 작업

**잔여 실측 방침**: 평소 스캔=MCP 가격만 빠르게(`ASOF_PRICE` 갱신). 브라우저 잔여 실측(`n`/`approx` 값, `ASOF_STOCK` 갱신)은 요청 시에만 2주치. 저가재고는 더 이상 컬럼으로 계산하지 않고 게하·굿데이 가격/만실 여부로 눈대중 판단.

매일 같은 시각에 `index.html`의 `TRACK` 객체를 갱신합니다:

1. **오늘 이전 날짜 삭제** — `TRACK`에서 실제 오늘보다 과거인 키를 지운다(이력은 git 커밋에 남음).
2. **가격 스캔(MCP, 빠르게)** — `TRACK[...].comp[*].p`와 `our.cur`를 갱신. 이 커밋에서는 **`ASOF_PRICE`만** 갱신한다.
3. **잔여 실측(요청 시에만, 브라우저 2주치)** — `comp[*].n`/`approx`·`our.ourN`을 갱신하고 `OURS_LOG`/`COMP_LOG`에 새 스냅샷을 추가(자동으로 최근 3개만 유지됨). 이 커밋에서는 **`ASOF_STOCK`만** 갱신한다.
4. 새 날짜(달이 넘어가 그 달 탭에 실제 데이터를 채울 때)를 `TRACK`에 추가할 때는 `dow`(한글 요일)·`t`(`wknd`/`wkdy`)를 실제 캘린더에 맞게 채운다.
5. 이벤트 캘린더(`EVENTS`/`POKER_EVENTS`/`POKER_BANDS`)를 점검·갱신하는 날에는 **`ASOF_EVENT`만** 갱신한다.
6. **세 기준일(`ASOF_PRICE`/`ASOF_STOCK`/`ASOF_EVENT`)은 서로 독립** — 한 종류를 갱신하는 커밋에서 나머지 둘은 절대 건드리지 않는다. 상단 날짜바는 오늘(`TODAY_KST`, 실제 시계) · 💰가격표 기준(경과 3일↑🟡·7일↑🔴) · 📦재고 기준(경과 14일↑🟡·30일↑🔴) · 🎤이벤트 기준(경과 14일↑🟡·30일↑🔴)을 각각 배지로 자동 표시한다.

같은 `index.html`을 덮어쓰기로 갱신해 URL을 유지하고, git 커밋으로 매일 이력을 자동 보존합니다.
